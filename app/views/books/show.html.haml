-# in app/views/books/show.html.haml
= button_to 'All Books', books_path, :method =>:get, :id => 'allbooks'
= button_to 'My Books', mybooks_path, :method =>:get, :id => 'mybooks'
%h2 #{@book.title}

%ul#details
  = image_tag asset_path(@book.image), :size => '75x100'
  %li
    Author:
    = @book.author
  %li
    Quality:
    = @book.quality
  %li
    Course:
    = @book.course
  %li
    Department:
    = @book.department
  %li
    Keywords:
    - @keywords.each_with_index do |keyword ,index|
      =keyword.tag
      -if index<@keywords.length-1
        ,

%h3 Description:

%p#description= @book.description

="Price $"+@book.price
= button_to 'Buy Now', buy_now_path(@book), :method=>:put

</br>

= form_tag make_bid_path, :method => :put do
  ="Auction Price $"+@book.bid_price
  </br>
  = "Sale ends in: "+@book.time_left 
  </br>
  = "$"
  = text_field :book, 'bid_price', :value=>""
  = submit_tag 'Bid'

